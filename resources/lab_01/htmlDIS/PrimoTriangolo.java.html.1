<!--
This file was generated by Web C Plus Plus software v0.7.9
Webcpp Copyright (C)2001, (C)2002 Jeffrey Bakker under the GNU GPL
Get webcpp at http://webcpp.sf.net
-->

<html>
<head>
<title>PrimoTriangolo.java</title>
<style type="text/css">

/** Webcpp v0.7.0+ compatible StyleSheet http://webcpp.sf.net **/
/** Theme: typical **/

body
{
background-color: #ffffff
}

a:link    {color:#ff0000}
a:visited {color:#666666}
a:active  {color:#0000ff}
a:hover   {color:#00b800}

pre
{
color: #000000
}

font
{
font-size:100%
}

font.preproc
{
color: #00b800
}

font.numbers
{
color: #a900a9
}

font.strings
{
color: #ff0000
}

font.keyword
{
color: #0000ff;
font-weight: bold
}

font.comment
{
color: #666666;
font-style: italic
}

</style>
</head>
<body>
<pre>

<font CLASS=keyword>import</font> java.lang.Math;
<font CLASS=keyword>import</font> java.awt.Point;


<font CLASS=keyword>public</font> <font CLASS=keyword>class</font> PrimoTriangolo {
	
	<font CLASS=keyword>public</font> PrimoTriangolo() {
		
		v1=<font CLASS=keyword>new</font> Point(X1,Y1);
		v2=<font CLASS=keyword>new</font> Point(X2,Y2);
		v3=<font CLASS=keyword>new</font> Point(X3,Y3);
			
		<font CLASS=keyword>double</font>	determinante=(v1.getX()-v3.getX())*(v2.getY()-v1.getY())-(v2.getX()-v1.getX())*(v1.getY()-v3.getY());
	
		<font CLASS=keyword>if</font>  (determinante==<font CLASS=numbers>0.0</font>)
			System.out.println(<font CLASS=strings>"Il triangolo e'degenere.\n"</font>);
			
	}
	
	<font CLASS=comment>/** 
	   Restituisce il perimetro del triangolo
	*/</font>
	<font CLASS=keyword>public</font> <font CLASS=keyword>double</font> perimetro(){
		
		<font CLASS=keyword>double</font> a=Math.sqrt(Math.pow(v1.getX()-v2.getX(),<font CLASS=numbers>2</font>)+Math.pow(v1.getY()-v2.getY(),<font CLASS=numbers>2</font>));
	    <font CLASS=keyword>double</font> b=Math.sqrt(Math.pow(v1.getX()-v3.getX(),<font CLASS=numbers>2</font>)+Math.pow(v1.getY()-v3.getY(),<font CLASS=numbers>2</font>));
	    <font CLASS=keyword>double</font> c=Math.sqrt(Math.pow(v2.getX()-v3.getX(),<font CLASS=numbers>2</font>)+Math.pow(v2.getY()-v3.getY(),<font CLASS=numbers>2</font>));
		<font CLASS=keyword>return</font> a+b+c;				
		
	}
	
	<font CLASS=comment>/** 
	   Restituisce l'area del triangolo
	*/</font>
	<font CLASS=keyword>public</font> <font CLASS=keyword>double</font> area (){
		
		<font CLASS=keyword>double</font> a=Math.sqrt(Math.pow(v1.getX()-v2.getX(),<font CLASS=numbers>2</font>)+Math.pow(v1.getY()-v2.getY(),<font CLASS=numbers>2</font>));
	    <font CLASS=keyword>double</font> b=Math.sqrt(Math.pow(v1.getX()-v3.getX(),<font CLASS=numbers>2</font>)+Math.pow(v1.getY()-v3.getY(),<font CLASS=numbers>2</font>));
	    <font CLASS=keyword>double</font> c=Math.sqrt(Math.pow(v2.getX()-v3.getX(),<font CLASS=numbers>2</font>)+Math.pow(v2.getY()-v3.getY(),<font CLASS=numbers>2</font>));
		<font CLASS=keyword>double</font> s=(a+b+c)/<font CLASS=numbers>2</font>;
		
		<font CLASS=keyword>return</font> Math.sqrt(s*(s-a)*(s-b)*(s-c));
		
	}
	
	<font CLASS=comment>/** 
	   Restituisce le coordinate (X,Y) del baricentro del triangolo
	*/</font>
	<font CLASS=keyword>public</font> Point baricentro(){
		
		Point baricentro=<font CLASS=keyword>new</font> Point((<font CLASS=keyword>int</font>)(v1.getX()+v2.getX()+v2.getX())/<font CLASS=numbers>3</font>,(<font CLASS=keyword>int</font>)(v1.getY()+v2.getY()+v3.getY())/<font CLASS=numbers>3</font>);
		
		<font CLASS=keyword>return</font> baricentro;
		
	}
	
	<font CLASS=keyword>private</font> <font CLASS=keyword>final</font> <font CLASS=keyword>int</font> X1=<font CLASS=numbers>100</font>, X2=<font CLASS=numbers>200</font>, X3=<font CLASS=numbers>300</font>, Y1=<font CLASS=numbers>200</font>, Y2=<font CLASS=numbers>400</font>, Y3=<font CLASS=numbers>600</font>; <font CLASS=comment>// contengono le coordinate dei vertici del triangolo</font>
	<font CLASS=keyword>private</font> Point v1, v2, v3;    <font CLASS=comment>// i vertici del triangolo</font>
	
}



</pre>



</body>
</html>
